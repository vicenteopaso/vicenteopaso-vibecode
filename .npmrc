# Suppress deprecation warnings for unavoidable transitive dependencies
# node-domexception is deprecated because Node.js 18+ has native DOMException
# It's a transitive dependency from contentlayer -> node-fetch -> fetch-blob
# and cannot be avoided without replacing contentlayer (which is unmaintained)
allowed-deprecated-versions=node-domexception=*
