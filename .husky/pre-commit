#!/usr/bin/env sh

# Run lint-staged with pnpm. Using pnpm directly here avoids relying on the
# standalone `corepack` CLI, which may not be available in GUI Git environments
# such as GitHub Desktop, while still respecting the configured package manager.
pnpm lint-staged
